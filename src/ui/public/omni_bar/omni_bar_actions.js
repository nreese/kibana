export const omniBarActions = {
  // TODO Comlex case - user click in omni bar calls callback
  // Example add element in canvas
  // global tells search if action should be displayed in global context
  // registerEventHandler: ({ key, searchText, appName, global, callback }) => {},

  search: (text) => {},
}
